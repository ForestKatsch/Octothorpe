
import OctoIRC
import XCTest

final class ClientTests: XCTestCase {
    // TODO: build test transport reply capabilities
    /*
     func testClientCreation() async throws {
         let transport = TestTransport()
         let options = RawClient.Options(.init(nickname: .init("DeltaOne"), realName: "Jason Bourne")!)
         let client = RawClient(transport, options: options)

         try await client.connect()

         XCTAssertEqual(transport.sent.count, 4)
     }
      */
}
